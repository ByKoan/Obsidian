Lo usaremos para ordenar con una condición
```sql
SELECT * FROM Products  
ORDER BY Price;
```

Si usamos la palabra clave `desc` se ordenara de orden mayor a menos:

```sql
SELECT * FROM Products
ORDER BY Price desc;
```
